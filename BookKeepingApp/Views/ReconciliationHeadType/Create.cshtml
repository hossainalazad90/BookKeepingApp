@using BookKeepingApp.Models.Enums;
@model BookKeepingApp.Models.ReconcilationHeadType

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Reconciliation Head Type</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Head" class="control-label"></label>
                <select asp-for="Head" class="form-control" asp-items="Html.GetEnumSelectList<HeadEnum>()" required></select>
                <span asp-validation-for="Head" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="TypeName" class="control-label"></label>
                <input asp-for="TypeName" class="form-control" required />
                <span asp-validation-for="TypeName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
